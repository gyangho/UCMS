<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>UCMS-면접 계획 상세</title>
    <%- include('../header') %>
    <link rel="stylesheet" href="/styles/style.css" />
    <style>
      body {
        margin: 0;
        font-family: "Noto Sans KR", sans-serif;
      }

      .container {
        max-width: 1200px;
        margin: 30px auto;
        padding: 20px;
      }

      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
      }

      .title {
        font-size: 2rem;
        color: #333;
        margin: 0;
      }

      .btn {
        padding: 12px 24px;
        font-size: 1rem;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-block;
      }

      .btn-primary {
        background: #667eea;
        color: white;
      }

      .btn-primary:hover {
        background: #5a6fd8;
      }

      .btn-secondary {
        background: #6c757d;
        color: white;
      }

      .btn-secondary:hover {
        background: #5a6268;
      }

      .plan-info-section {
        background: white;
        border: 2px solid #e0e0e0;
        border-radius: 12px;
        padding: 25px;
        margin-bottom: 25px;
      }

      .section-title {
        font-size: 1.4rem;
        font-weight: bold;
        color: #333;
        margin-bottom: 20px;
        padding-bottom: 10px;
        border-bottom: 2px solid #667eea;
      }

      .info-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 20px;
      }

      .info-item {
        display: flex;
        flex-direction: column;
      }

      .info-label {
        font-weight: bold;
        color: #666;
        margin-bottom: 5px;
        font-size: 0.9rem;
      }

      .info-value {
        color: #333;
        font-size: 1.1rem;
      }

      .dates-section {
        background: white;
        border: 2px solid #e0e0e0;
        border-radius: 12px;
        padding: 25px;
        margin-bottom: 25px;
      }

      .dates-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 15px;
      }

      .date-card {
        background: #f8f9fa;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        padding: 15px;
        text-align: center;
      }

      .date-label {
        font-weight: bold;
        color: #495057;
        margin-bottom: 8px;
      }

      .date-value {
        color: #667eea;
        font-size: 1.1rem;
        font-weight: bold;
      }

      .interviewers-section {
        background: white;
        border: 2px solid #e0e0e0;
        border-radius: 12px;
        padding: 25px;
        margin-bottom: 25px;
      }

      .interviewers-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 15px;
      }

      .interviewer-card {
        background: #f8f9fa;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        padding: 15px;
        text-align: center;
      }

      .interviewer-name {
        font-weight: bold;
        color: #495057;
        margin-bottom: 5px;
      }

      .interviewer-id {
        color: #6c757d;
        font-size: 0.9rem;
      }

      .time-slots-section {
        background: white;
        border: 2px solid #e0e0e0;
        border-radius: 12px;
        padding: 25px;
        margin-bottom: 25px;
      }

      .time-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 15px;
      }

      .time-table th,
      .time-table td {
        border: 1px solid #dee2e6;
        padding: 10px;
        text-align: center;
      }

      .time-table th {
        background: #f8f9fa;
        font-weight: bold;
        color: #495057;
      }

      .time-slot-available {
        background: #d4edda;
        color: #155724;
        font-weight: bold;
      }

      .time-slot-unavailable {
        background: #f8d7da;
        color: #721c24;
        font-weight: bold;
      }

      .back-link {
        color: #667eea;
        text-decoration: none;
        font-weight: bold;
        display: inline-flex;
        align-items: center;
        gap: 5px;
      }

      .back-link:hover {
        text-decoration: underline;
      }

      @media (max-width: 768px) {
        .header {
          flex-direction: column;
          gap: 15px;
          align-items: flex-start;
        }

        .info-grid {
          grid-template-columns: 1fr;
        }

        .dates-grid,
        .interviewers-grid {
          grid-template-columns: 1fr;
        }

        .time-table {
          font-size: 0.9rem;
        }

        .time-table th,
        .time-table td {
          padding: 8px 5px;
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <div>
          <a href="/recruit/interview/plans" class="back-link"
            >← 면접 계획 목록으로</a
          >
          <h1 class="title"><%= plan.title %></h1>
        </div>
        <div>
          <a
            href="/recruit/interview/plan/selectform?planId=<%= plan.id %>"
            class="btn btn-primary"
            >편집</a
          >
        </div>
      </div>

      <!-- 기본 정보 섹션 -->
      <div class="plan-info-section">
        <h2 class="section-title">기본 정보</h2>
        <div class="info-grid">
          <div class="info-item">
            <span class="info-label">면접 계획 제목</span>
            <span class="info-value"><%= plan.title %></span>
          </div>
          <div class="info-item">
            <span class="info-label">연결된 폼</span>
            <span class="info-value">
              <a
                href="https://docs.google.com/forms/d/<%= plan.form_id %>/edit"
                target="_blank"
                style="color: #667eea; text-decoration: none"
              >
                <%= plan.form_title %>
              </a>
            </span>
          </div>
          <div class="info-item">
            <span class="info-label">생성자</span>
            <span class="info-value"
              ><%= plan.created_by_name %></span
            >
          </div>
          <div class="info-item">
            <span class="info-label">최종 수정자</span>
            <span class="info-value"
              ><%= plan.updated_by_name %></span
            >
          </div>
          <div class="info-item">
            <span class="info-label">생성일</span>
            <span class="info-value"
              ><%= new
              Date(plan.created_at).toLocaleDateString('ko-KR')
              %></span
            >
          </div>
          <div class="info-item">
            <span class="info-label">최종 수정일</span>
            <span class="info-value"
              ><%= new
              Date(plan.updated_at).toLocaleDateString('ko-KR')
              %></span
            >
          </div>
        </div>
      </div>

      <!-- 면접 날짜 섹션 -->
      <div class="dates-section">
        <h2 class="section-title">면접 날짜</h2>
        <% if (interviewDates && interviewDates.length > 0) { %>
        <div class="dates-grid">
          <% interviewDates.forEach(date => { %>
          <div class="date-card">
            <div class="date-label">면접 날짜</div>
            <div class="date-value">
              <%= new
              Date(date.interview_date).toLocaleDateString('ko-KR') %>
            </div>
          </div>
          <% }) %>
        </div>
        <% } else { %>
        <p style="color: #6c757d; text-align: center">
          등록된 면접 날짜가 없습니다.
        </p>
        <% } %>
      </div>

      <!-- 면접관 섹션 -->
      <div class="interviewers-section">
        <h2 class="section-title">면접관</h2>
        <% if (interviewers && interviewers.length > 0) { %>
        <div class="interviewers-grid">
          <% interviewers.forEach(interviewer => { %>
          <div class="interviewer-card">
            <div class="interviewer-name">
              <%= interviewer.name %>
            </div>
            <div class="interviewer-id"><%= interviewer.id %></div>
          </div>
          <% }) %>
        </div>
        <% } else { %>
        <p style="color: #6c757d; text-align: center">
          등록된 면접관이 없습니다.
        </p>
        <% } %>
      </div>

      <!-- 면접 시간대 섹션 -->
      <div class="time-slots-section">
        <h2 class="section-title">면접 시간대</h2>
        <% if (interviewerTimeSlots && interviewerTimeSlots.length >
        0) { %>
        <table class="time-table">
          <thead>
            <tr>
              <th>면접관</th>
              <th>면접 날짜</th>
              <th>시간대</th>
              <th>가능 여부</th>
            </tr>
          </thead>
          <tbody>
            <% interviewerTimeSlots.forEach(slot => { %>
            <tr>
              <td>
                <%= slot.interviewer_name || slot.interviewer_id %>
              </td>
              <td>
                <%= new
                Date(slot.interview_date).toLocaleDateString('ko-KR')
                %>
              </td>
              <td><%= slot.time_slot %></td>
              <td
                class="<%= slot.is_available ? 'time-slot-available' : 'time-slot-unavailable' %>"
              >
                <%= slot.is_available ? '가능' : '불가능' %>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } else { %>
        <p style="color: #6c757d; text-align: center">
          등록된 면접 시간대가 없습니다.
        </p>
        <% } %>
      </div>
    </div>

    <script>
      // 페이지 로드 시 면접관 이름 정보 가져오기
      document.addEventListener('DOMContentLoaded', function() {
        // 면접관 이름을 시간대 테이블에 표시하기 위한 처리
        const timeTable = document.querySelector('.time-table');
        if (timeTable) {
          const rows = timeTable.querySelectorAll('tbody tr');
          rows.forEach(row => {
            const interviewerId = row.cells[0].textContent;
            // 면접관 ID가 숫자인 경우 이름으로 변경
            if (!isNaN(interviewerId)) {
              const interviewer = <%= JSON.stringify(interviewers) %>.find(i => i.id === interviewerId);
              if (interviewer) {
                row.cells[0].textContent = interviewer.name;
              }
            }
          });
        }
      });
    </script>
  </body>
</html>
